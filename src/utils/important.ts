export const doSomethingImportantWithTheValue = (value: any) => {
  // We take the value and do something with it. It does not matter what we do,
  // but it's crucial that this function is called.
}
